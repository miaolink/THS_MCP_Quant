# 快速开始

RHTHS 使用同花顺进程内**原生 `ths_api` 标准调用**（**非 WinGUI 键鼠模拟**；**不含逆向破解、不破解同花顺软件**），安装 Hook 后查询/模拟无第三方 API 配额限制。合规说明见 [README.md](./README.md#技术与合规说明合法使用)。

## 你需要准备

1. **Windows** + 任一同花顺 **PC 客户端**（免费版 / 金融大师 / 远航版 / 券商定制版 / 独立下单 xiadan 等，能登录交易即可）——**无需向同花顺/券商/第三方申请或开通任何外部 API 授权**
2. 已安装 **RHTHS** 并完成 Hook 部署（安装包默认流程或 GUI「安装/更新 Hook」）
3. **同花顺（或独立下单）已启动并登录**交易账户

> **交易能力只存在于安装了同花顺的那台机器。** CLI 只能在这台机器上用；MCP 可以让其它电脑上的 AI 通过局域网连过来（见 [MCP使用说明.md](./MCP使用说明.md) 第三节）。

## 一分钟检查

在**交易机** PowerShell 中（把路径改成你的安装目录）：

```powershell
$dist = "D:\allinpol\RHTHS\dist"
& "$dist\rhths.exe" health
```

若返回 JSON 里 `ok` 为 `true` 且 `data.ready` 为 `true`，说明网关正常，可继续用 **CLI** 或 **MCP**。

## 推荐用法

| 场景 | 用什么 | 运行位置 |
|------|--------|----------|
| 本机脚本、批处理、定时任务 | [CLI使用说明.md](./CLI使用说明.md) | **仅交易机** |
| 本机 Cursor / OpenClaw / Hermes Agent 等 AI 助手 | [MCP使用说明.md](./MCP使用说明.md) § 二 本机 stdio | AI 与交易在同一台 PC |
| 笔记本 / 办公室电脑上的 AI，交易在家里的电脑 | [MCP使用说明.md](./MCP使用说明.md) § 三 局域网 HTTP | 交易机开 MCP HTTP，客户端填局域网 IP |
| 图形界面、激活高级版、检查更新 | `rhths-gui.exe` | 交易机 |

## 软件版本（标准版 / 高级版）

- **标准版**（默认）：功能全开；**实盘**买入+卖出合计每天最多 **10 次**；**模拟**下单不限次数。
- **高级版**：在 GUI「激活」页用注册码激活后，实盘无次数限制。

查询、行情、模拟下单在标准版下均不受限。

## 常见问题

| 现象 | 处理 |
|------|------|
| 连接 `19312` 失败 | 确认同花顺已开；在 GUI 里「安装/更新 Hook」后重启 xiadan |
| MCP 工具列表为空 | 检查 `mcp.json` 路径、Reload Window / 重启客户端；远程模式先 `curl http://交易机IP:19310/health` |
| 远程 MCP 连不上 | 防火墙放行 **19310**；确认交易机与客户端在同一局域网 |
| 实盘下单被拒 | 默认仅模拟；见 MCP/CLI 文档中的「实盘」章节 |
